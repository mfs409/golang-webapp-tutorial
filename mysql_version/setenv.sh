# This script is a convenience.  When working on Windows using 'git bash' as
# the shell, we can enter the golang-webapp-tutorial folder and then type
# '. setenv.sh' to set our GOPATH.  Doing so ensures that 'go build ...'
# works as desired.
export GOPATH=`pwd`
